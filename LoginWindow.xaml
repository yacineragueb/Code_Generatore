<Window x:Class="Code_Generatore.LoginWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Code_Generatore"
        mc:Ignorable="d"
        Title="Code generator" Height="400" Width="740"
        WindowStartupLocation="CenterScreen" ResizeMode="NoResize"
        >

    <Window.Resources>
        <BooleanToVisibilityConverter x:Key="BooleanToVisibilityConverter"/>
    </Window.Resources>

    <DockPanel>

        <!-- #region Left Side -->
        <Grid DockPanel.Dock="Left" Width="300" Background="#4895ef">
            <Grid.RowDefinitions>
                <RowDefinition Height="*" />
                <RowDefinition Height="Auto" />
            </Grid.RowDefinitions>

            <Image Grid.Row="0"
                   Width="200"
                   HorizontalAlignment="Center"
                   VerticalAlignment="Center"
                   Source="/Resources/Images/Logo-white.png" />   

            <Label Grid.Row="1" Foreground="White" Content="Version 1.0.0" HorizontalAlignment="Center" FontSize="16" FontWeight="Medium" Margin="0,0,0,20" />
        </Grid>
        <!--#endregion-->

        <!-- #region Right Side --> 
        <StackPanel DockPanel.Dock="Right">
            <TextBlock Text="Please enter your SQL Server username and password to establish a connection."
                       Margin="5"
                       Style="{StaticResource DescriptionText}"/>

            <Grid Height="Auto" >
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="Auto" />
                </Grid.RowDefinitions>

                <StackPanel Grid.Row="0" >
                    <Label Content="Username:" />
                    <TextBox x:Name="UsernameTextBox" Text="{Binding Username, UpdateSourceTrigger=PropertyChanged}" />
                </StackPanel>

                <StackPanel Grid.Row="1" >
                    <Label Content="Password:" />
                    <PasswordBox x:Name="PasswordBox" PasswordChanged="PasswordBox_PasswordChanged"/>
                    <TextBox x:Name="PasswordUnmaskTextBox" Visibility="Collapsed" TextChanged="PasswordUnmaskTextBox_TextChanged"/>

                    <Grid Margin="5">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*" />
                            <ColumnDefinition Width="*" />
                        </Grid.ColumnDefinitions>
                        
                        <CheckBox Grid.Column="0"
                                  Content="Show Password"
                                  HorizontalAlignment="Left"
                                  x:Name="ShowPasswordCheckbox"
                                  Checked="ShowPasswordCheckbox_Checked"
                                  Unchecked="ShowPasswordCheckbox_Unchecked"/>

                        <CheckBox Grid.Column="1"
                                  Content="Remember me"
                                  HorizontalAlignment="Right"
                                  IsChecked="{Binding RememberMe}"/>
                    </Grid>
                </StackPanel>

                <TextBlock Grid.Row="2"
                           d:Visibility="Visible"
                           d:Text="Username and Password are required."
                           Text="{Binding ErrorMessage}"
                           Style="{StaticResource ErrorMessageStyle}"
                           Visibility="{Binding HasError, Converter={StaticResource BooleanToVisibilityConverter}}"/>

                <Button Grid.Row="3"
                        Style="{StaticResource PrimaryBtn}"
                        Content="Connect"
                        Command="{Binding LoginCommand}" />

            </Grid>
        </StackPanel>
        <!--#endregion-->
        
    </DockPanel>
</Window>
